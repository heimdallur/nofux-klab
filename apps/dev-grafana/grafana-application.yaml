spec:
  source:
    repoURL: https://github.com/heimdallur/nofux-klab
    targetRevision: main               # your Git branch
    path: charts/charts/grafana
    helm:
      valueFiles:
        - values.yaml
      # values: |
      #   adminUser: klab
      #   adminPassword: strongpassword

      #   grafana.ini:
      #     server:
      #       http_port: 8055

      #   service:
      #     type: LoadBalancer
      #     loadBalancerIP: "192.168.68.75"
      #     annotations:
      #       metallb.universe.tf/address-pool: cheap
      #       metallb.universe.tf/allow-shared-ip: "true"
      #     port: 8055
      #     targetPort: 8055
      #     protocol: TCP
      #     name: http
