persistence:
  enabled: true
  size: 5Gi

adminUser: klab
adminPassword: {{ secrets.GRAFANA_PASSWORD }}

service:
  type: LoadBalancer

grafana.ini:
  server:
    http_port: 8055
    instance_name: "Klab Grafana"
    